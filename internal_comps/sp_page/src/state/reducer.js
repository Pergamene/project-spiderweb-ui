import * as ActionType from './action.js';

const INITIAL_STATE = {
  ui: {},
  entities: {}
};

export const sp_page = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
